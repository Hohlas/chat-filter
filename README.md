# 🤖 Telegram Chat Analyzer

Telegram userbot для анализа сообщений чата с помощью Perplexity AI. Бот работает от вашего аккаунта через MTProto API и создает структурированные выжимки сообщений за указанный период.

## 📚 Документация

- ⚡ [QUICKSTART.md](QUICKSTART.md) - **Быстрый старт за 5 минут** (начните здесь!)
- 📖 [README.md](README.md) - Основная документация (вы здесь)
- 📝 [USAGE_EXAMPLES.md](USAGE_EXAMPLES.md) - Примеры использования и сценарии
- 🔧 [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение типичных проблем
- 📋 [CHEATSHEET.md](CHEATSHEET.md) - Быстрая шпаргалка по командам
- 📜 [CHANGELOG.md](CHANGELOG.md) - История изменений проекта
- 🤝 [CONTRIBUTING.md](CONTRIBUTING.md) - Руководство для контрибьюторов

## ✨ Возможности

- 📊 Анализ сообщений чата за любой период времени (часы/дни)
- 🤖 Создание структурированной выжимки с помощью Perplexity AI
- 💬 Работа в любом чате, к которому у вас есть доступ
- 💾 Автоматическое сохранение результатов в JSON файлы
- 🔐 Безопасное хранение API ключей (не попадают в git)

## 🚀 Установка

### Шаг 1: Получение API ключей

#### Telegram API:

1. Перейдите на https://my.telegram.org/auth
2. Войдите с вашим номером телефона
3. Выберите "API development tools"
4. Заполните App title и Short name
5. Получите `api_id` и `api_hash`

#### Perplexity API:

1. Зайдите на https://www.perplexity.ai/settings/api
2. Зарегистрируйте платёжную информацию
3. Пополните баланс кредитами (API-ключи генерируются только при ненулевом балансе)
4. Сгенерируйте и скопируйте API ключ

### Шаг 2: Клонирование репозитория

```bash
git clone <url-репозитория>
cd chat-filter
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка конфигурации

Создайте файл `private.txt` в корне проекта и заполните его:

```
# Telegram API
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_PHONE=+79001234567

# Perplexity API
PERPLEXITY_API_KEY=ваш_perplexity_ключ

# ID чата для мониторинга (необязательно, можно удалить)
CHAT_ID=123456789
```

**Примечание:** Файл `private.txt` уже добавлен в `.gitignore` и не будет синхронизироваться через git.

## 📝 Использование

### Запуск бота

```bash
python main.py
```

При первом запуске Telegram попросит ввести код подтверждения, отправленный на ваш номер телефона.

### Команды бота

После запуска бот ожидает команд в любом чате Telegram. Команды нужно отправлять **от своего имени** (outgoing messages).

#### `/analyze` - Анализ чата за последние 24 часа

```
/analyze
```

#### `/analyze [время]` - Анализ за указанный период

Примеры:

```
/analyze 12h          # За последние 12 часов
/analyze 2d           # За последние 2 дня
/analyze 3d 6h        # За последние 3 дня и 6 часов
```

#### `/help` - Справка по командам

```
/help
```

### Как это работает

1. Отправьте команду `/analyze` в нужный чат
2. Бот соберет все сообщения за указанный период
3. Отправит их в Perplexity AI для анализа
4. Получит структурированную выжимку, включающую:
   - Основные темы обсуждения
   - Ключевые решения или выводы
   - Важные факты, даты, цифры
   - Упоминания о задачах или действиях
   - Спорные моменты или вопросы
5. Отправит результат вам в этот же чат

## 📁 Структура проекта

```
chat-filter/
├── main.py                    # Основной файл бота
├── requirements.txt           # Зависимости Python
├── private.txt                # Конфигурация (не в git)
├── .gitignore                 # Исключения для git
├── README.md                  # Документация
│
├── start_bot.sh               # Скрипт запуска бота
├── start_bot_background.sh    # Скрипт запуска в фоновом режиме
├── stop_bot.sh                # Скрипт остановки бота
│
├── session_name.session       # Сессия Telegram (создается автоматически, не в git)
└── analysis_*.json            # Результаты анализа (создаются автоматически, не в git)
```

## 🔧 Настройка промта для анализа

Вы можете настроить промт для анализа в файле `main.py`, изменив переменную `ANALYSIS_PROMPT`:

```python
ANALYSIS_PROMPT = """Ваш промт для анализа сообщений"""
```

## 🖥️ Развертывание на Ubuntu сервере

### На локальном компьютере:

1. Разработайте и протестируйте бота
2. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Update bot"
   git push
   ```

### На Ubuntu сервере:

1. Клонируйте репозиторий:
   ```bash
   git clone <url-репозитория>
   cd chat-filter
   ```

2. Создайте файл `private.txt` с вашими ключами

3. Сделайте скрипты исполняемыми:
   ```bash
   chmod +x start_bot.sh start_bot_background.sh stop_bot.sh
   ```

4. Запустите бота:
   
   **Обычный режим:**
   ```bash
   ./start_bot.sh
   ```
   
   **Фоновый режим (рекомендуется для сервера):**
   ```bash
   ./start_bot_background.sh
   ```

5. Управление ботом в фоновом режиме:
   ```bash
   # Подключиться к работающему боту
   screen -r telegram-chat-analyzer
   
   # Отключиться от бота (Ctrl+A, затем D)
   # Бот продолжит работать в фоне
   
   # Остановить бота
   ./stop_bot.sh
   
   # Посмотреть логи (если бот запущен в фоне)
   screen -r telegram-chat-analyzer
   ```

### Альтернативный способ с systemd (автозапуск):

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Chat Analyzer Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/chat-filter
ExecStart=/usr/bin/python3 /path/to/chat-filter/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

## ⚠️ Важные замечания

- **Безопасность:** Никогда не коммитьте файл `private.txt` в git!
- **Первый запуск:** При первом запуске на новой машине потребуется аутентификация через Telegram
- **Файл сессии:** Файл `*.session` содержит данные авторизации, он также не должен попадать в git
- **Лимиты API:** Следите за использованием Perplexity API, чтобы не превысить лимиты
- **Ограничения:** Максимальный размер анализируемого текста ограничен 15000 символов (настраивается в коде)

## 📊 Результаты анализа

Результаты сохраняются в JSON файлы с именами вида `analysis_YYYYMMDD_HHMMSS.json`:

```json
{
  "timestamp": "2024-01-01T12:00:00",
  "messages_count": 150,
  "messages": [
    {
      "sender": "Иван Иванов",
      "text": "Текст сообщения",
      "date": "2024-01-01 11:30:00"
    }
  ],
  "summary": "Структурированная выжимка от Perplexity AI"
}
```

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Правильность API ключей в `private.txt`
2. Наличие всех зависимостей из `requirements.txt`
3. Подключение к интернету
4. Логи в консоли при запуске бота

## 📄 Лицензия

Проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).
