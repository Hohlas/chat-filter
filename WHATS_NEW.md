# 🎉 Что нового в версии 2.0.0

## 🚀 Главные улучшения

### 1. 📁 Конфигурационные файлы
Теперь вся настройка вынесена в удобные текстовые файлы:
- `EXCLUDED_USERS.txt` - пользователи для исключения
- `PRIORITY_USERS.txt` - приоритетные пользователи
- `PROMPT.txt` - промпт для Perplexity API

**Больше не нужно редактировать код Python!**

### 2. 🎮 Управление через Telegram
Редактируйте конфигурацию прямо из мессенджера:

```
/config                    # Посмотреть настройки
/add_excluded SpamBot      # Добавить в исключенные
/add_priority Expert123    # Добавить в приоритетные
/reload_config             # Перезагрузить из файлов
```

### 3. 📂 Автоматические темы
Бот автоматически создает темы в вашем форум-канале по названию чата:
- "Bitcoin Discussion" → тема "Bitcoin Discussion"
- "Concordium_RU" → тема "Concordium_RU"

Все анализы организованы и легко найти!

### 4. 💰 Экономия токенов API
Умная фильтрация сообщений экономит 20-40% токенов:
- ❌ Исключаются указанные пользователи
- ❌ Удаляется технический флуд (+, ок, лол)
- ❌ Фильтруются бессодержательные сообщения
- ✅ Остается только полезный контент

**Пример статистики:**
```
✅ Оптимизация завершена:
   • Исходно: 150 сообщений
   • Исключено пользователей: 12
   • Удалено шума/флуда: 38
   • Итого для анализа: 100 сообщений
   • Экономия: 50 сообщений (33.3%)
```

### 5. 🔗 Прямые ссылки на сообщения
Теперь в анализе каждое упоминание пользователя - это ссылка:
```
[Zinur](https://t.me/c/1234567/890): предложил использовать новый протокол
```
Клик → переход к оригинальному сообщению!

## 📋 Новые команды

| Команда | Описание |
|---------|----------|
| `/config` | Показать текущую конфигурацию |
| `/show_excluded` | Список исключенных пользователей |
| `/show_priority` | Список приоритетных пользователей |
| `/show_prompt` | Показать текущий промпт |
| `/add_excluded username` | Добавить в исключенные |
| `/remove_excluded username` | Убрать из исключенных |
| `/add_priority username` | Добавить в приоритетные |
| `/remove_priority username` | Убрать из приоритетных |
| `/reload_config` | Перезагрузить конфигурацию |

## 🎯 Быстрый старт

### Шаг 1: Обновите код
```bash
cd ~/chat-filter
git pull
```

### Шаг 2: Включите темы (опционально)
В вашем Telegram канале:
1. Управление каналом → Темы → Включить

### Шаг 3: Запустите бота
```bash
python3 main.py
```

### Шаг 4: Проверьте конфигурацию
В любом чате Telegram отправьте:
```
/config
```

### Шаг 5: Настройте под себя
```
/add_excluded FlooderName
/add_priority ExpertName
```

### Шаг 6: Анализируйте!
```
/analyze 12h
```

## 💡 Полезные сценарии

### Быстро исключить спамера
```
/add_excluded SpammerNick
/analyze 6h
```

### Добавить нового эксперта
```
/add_priority NewExpert
/show_priority
```

### Обновить промпт
1. Отредактируйте `PROMPT.txt` на сервере или локально
2. Если локально: `git push` → на сервере: `git pull`
3. В Telegram: `/reload_config`
4. Проверьте: `/show_prompt`

### Массовое обновление списков
1. Отредактируйте `EXCLUDED_USERS.txt` и `PRIORITY_USERS.txt`
2. Закоммитьте и запушьте изменения
3. На сервере: `git pull`
4. В Telegram: `/reload_config`
5. Проверьте: `/config`

## 📚 Документация

- **CONFIG_MANAGEMENT.md** - полное руководство по управлению конфигурацией
- **CHANGELOG.md** - детальная история изменений
- **README.md** - основная документация

## ⚠️ Важно знать

1. **Имена с пробелами** поддерживаются: `Restyle Pon` ✅
2. **Регистр важен**: `Alice` ≠ `alice`
3. **Автосохранение** при использовании команд бота
4. **UTF-8** кодировка для файлов (для кириллицы)
5. После редактирования файлов напрямую используйте `/reload_config`

## 🎊 Наслаждайтесь новыми возможностями!

Теперь управление ботом стало намного удобнее и гибче. Все настройки - в простых текстовых файлах, редактируйте откуда угодно!

---

**Вопросы?** Проверьте CONFIG_MANAGEMENT.md или отправьте `/help` в Telegram

