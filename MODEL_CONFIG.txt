# Конфигурация модели Perplexity API
# Можно редактировать вручную или через команды бота

# ⚠️ ВАЖНО: Через Perplexity API доступны ТОЛЬКО модели Sonar!
# Claude, GPT и другие модели доступны только в веб-интерфейсе Perplexity Pro

# Доступные модели через API:
# - sonar (базовая модель, на основе Llama 3.3 70B)
# - sonar-pro (улучшенная версия с лучшим качеством)

MODEL=sonar-pro

# Использовать ли режим reasoning (экспериментально)
# true или false
USE_REASONING=false

# Использовать HTML файлы вместо Telegraph
# true - создавать локальные HTML файлы и отправлять в Telegram
# false - публиковать на Telegraph (требует интернет-соединение)
USE_HTML_EXPORT=true

