# 🔄 Руководство по обновлению до версии 2.0.0

## Шаг 1: Обновление кода

### На локальном компьютере (Windows):
```bash
cd C:\Users\hohla\git\chat-filter
git add .
git commit -m "Update to v2.0.0"
git push
```

### На удаленном сервере (Ubuntu):
```bash
cd ~/chat-filter
git pull
```

## Шаг 2: Проверка новых файлов

После `git pull` должны появиться новые файлы:
```bash
ls -la *.txt *.md
```

Ожидаемые файлы:
- ✅ `EXCLUDED_USERS.txt`
- ✅ `PRIORITY_USERS.txt`
- ✅ `PROMPT.txt`
- ✅ `CONFIG_MANAGEMENT.md`
- ✅ `WHATS_NEW.md`
- ✅ `UPDATE_GUIDE.md`

## Шаг 3: Остановка старого бота (если запущен)

```bash
# Если бот запущен в screen
./stop_bot.sh

# Или найдите процесс вручную
ps aux | grep main.py
kill <PID>
```

## Шаг 4: Запуск нового бота

```bash
# Обычный режим (для тестирования)
python3 main.py

# Или в фоновом режиме
./start_bot_background.sh
```

При запуске вы должны увидеть:
```
🚀 Запуск Telegram бота для анализа чатов...
============================================================
✅ Подключение к Telegram установлено

📮 Результаты будут отправляться в: приватный канал
   ID канала: -1002958350329
   ✅ Канал найден: Chat Summary
   📁 Форум включен: темы будут создаваться автоматически

🎯 Настройки оптимизации:
   • Исключенные пользователи: AntonHoldon, gtslay, E2ard, vlad_calista
   • Приоритетные пользователи: Zinur, Restyle Pon, Lex, ProMint, Sergey
   • Минимальная длина сообщения: 3 символов

📌 Доступные команды:
  Анализ:
    /analyze - анализ чата за последние 24 часа
    /analyze [время] - анализ за указанный период
  Конфигурация:
    /config - показать конфигурацию
    /add_excluded, /remove_excluded - управление исключенными
    /add_priority, /remove_priority - управление приоритетными
    /reload_config - перезагрузить из файлов
  Справка:
    /help - полная справка по командам

👀 Ожидание команд...
```

## Шаг 5: Проверка работы

### В Telegram отправьте:
```
/config
```

Вы должны увидеть текущую конфигурацию с загруженными списками пользователей.

### Тестовый анализ:
Выберите небольшой чат и отправьте:
```
/analyze 1h
```

Проверьте:
1. ✅ Создалась ли тема с названием чата?
2. ✅ Работает ли фильтрация (проверьте статистику)?
3. ✅ Получен ли анализ от Perplexity?

## Шаг 6: Настройка под себя

### Добавить/убрать пользователей:
```
/add_excluded NewSpammer
/remove_excluded OldUser
/show_excluded
```

### Проверить промпт:
```
/show_prompt
```

### Если нужно изменить промпт:

**Вариант 1: На сервере**
```bash
nano PROMPT.txt
# Редактируем, сохраняем
```
В Telegram: `/reload_config`

**Вариант 2: Локально + Git**
1. Редактируем `PROMPT.txt` в IDE
2. `git add PROMPT.txt && git commit -m "Update prompt" && git push`
3. На сервере: `git pull`
4. В Telegram: `/reload_config`

## 🎯 Включение тем в канале (если еще не включено)

1. Откройте ваш канал "Chat Summary" в Telegram
2. Нажмите на название канала → Управление каналом
3. Найдите опцию "Темы" (Topics)
4. Включите её
5. Перезапустите бота или подождите следующего анализа

Теперь каждый чат будет иметь свою тему!

## ⚠️ Возможные проблемы

### Проблема: "Файл не найден"
```
⚠️  Файл EXCLUDED_USERS.txt не найден, используется пустой список
```

**Решение:**
```bash
cd ~/chat-filter
ls -la *.txt  # Проверьте наличие файлов
git pull      # Убедитесь что все файлы загружены
```

### Проблема: Бот не создает темы
```
ℹ️  Форум не включен: все сообщения в общий чат
```

**Решение:** Включите темы в настройках канала (см. выше)

### Проблема: Ошибка при создании темы
```
❌ Ошибка при создании темы: ...
```

**Решение:**
1. Убедитесь что вы админ канала
2. Проверьте что форум включен
3. Проверьте права бота в канале

### Проблема: Конфигурация не обновляется
**Решение:**
```
/reload_config
```

## 📊 Проверка работы оптимизации

После анализа чата вы должны видеть статистику:
```
🔄 Оптимизация 150 сообщений...
✅ Оптимизация завершена:
   • Исходно: 150 сообщений
   • Исключено пользователей: 12
   • Удалено шума/флуда: 38
   • Итого для анализа: 100 сообщений
   • Экономия: 50 сообщений (33.3%)
```

Если экономия меньше 10%, возможно нужно:
- Добавить больше пользователей в EXCLUDED_USERS.txt
- Проверить что в чате действительно есть шум

## 🎉 Готово!

Бот обновлен и готов к работе! 

### Следующие шаги:
1. Протестируйте на нескольких чатах
2. Настройте списки пользователей под ваши чаты
3. При необходимости адаптируйте промпт
4. Наслаждайтесь экономией токенов!

### Полезные ссылки:
- **CONFIG_MANAGEMENT.md** - подробное управление конфигурацией
- **WHATS_NEW.md** - обзор новых возможностей
- **CHANGELOG.md** - полная история изменений

---

**Нужна помощь?** Отправьте `/help` в Telegram для полной справки по командам.

