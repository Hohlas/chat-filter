# 📋 Шпаргалка по командам

Быстрый справочник для работы с Telegram Chat Analyzer.

---

## 🤖 Команды бота в Telegram

| Команда | Описание | Пример |
|---------|----------|--------|
| `/analyze` | Анализ за 24 часа | `/analyze` |
| `/analyze Xh` | Анализ за X часов | `/analyze 12h` |
| `/analyze Xd` | Анализ за X дней | `/analyze 7d` |
| `/analyze Xd Yh` | Комбинированный период | `/analyze 3d 6h` |
| `/help` | Справка по командам | `/help` |

---

## 💻 Команды терминала

### Установка и настройка

```bash
# Клонировать репозиторий
git clone <url>
cd chat-filter

# Установить зависимости
pip install -r requirements.txt

# Создать конфигурацию
cp private.txt.example private.txt
nano private.txt
```

### Запуск бота

```bash
# Обычный режим
python main.py

# С использованием скрипта
./start_bot.sh

# Фоновый режим
./start_bot_background.sh

# Остановка
./stop_bot.sh
```

### Управление через screen

```bash
# Подключиться к боту
screen -r telegram-chat-analyzer

# Отключиться (Ctrl+A, затем D)
# Бот продолжит работать

# Список сессий
screen -ls

# Завершить сессию
screen -S telegram-chat-analyzer -X quit
```

### Управление через systemd

```bash
# Запуск
sudo systemctl start telegram-bot

# Остановка
sudo systemctl stop telegram-bot

# Перезапуск
sudo systemctl restart telegram-bot

# Статус
sudo systemctl status telegram-bot

# Логи
sudo journalctl -u telegram-bot -f

# Автозапуск
sudo systemctl enable telegram-bot
```

---

## 📂 Структура файлов

```
chat-filter/
├── main.py                 # ⚙️  Основной файл
├── requirements.txt        # 📦 Зависимости
├── private.txt            # 🔐 Конфигурация (не в git)
├── README.md              # 📖 Документация
├── USAGE_EXAMPLES.md      # 📝 Примеры использования
├── TROUBLESHOOTING.md     # 🔧 Решение проблем
├── CHEATSHEET.md          # 📋 Эта шпаргалка
├── start_bot.sh           # ▶️  Запуск
├── start_bot_background.sh # 🔄 Фоновый запуск
├── stop_bot.sh            # ⏹️  Остановка
├── session_name.session   # 💾 Сессия (создается авто)
└── analysis_*.json        # 📊 Результаты (создаются авто)
```

---

## 🔑 Формат private.txt

```bash
# Telegram API
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
TELEGRAM_PHONE=+79001234567

# Perplexity API
PERPLEXITY_API_KEY=pplx-1234567890abcdef

# Опционально
CHAT_ID=123456789
```

---

## ⚙️ Основные настройки в main.py

### Промт для анализа
```python
ANALYSIS_PROMPT = """Ваш промт"""
```

### Лимит символов
```python
max_chars = 15000  # В функции create_summary
```

### Лимит токенов ответа
```python
max_tokens=2000  # В запросе к Perplexity
```

### Модель Perplexity
```python
model='sonar'  # Или другая модель
```

---

## 🚀 Быстрый старт

### Локально (Windows/Mac/Linux)

```bash
# 1. Клонировать и перейти
git clone <url> && cd chat-filter

# 2. Установить
pip install -r requirements.txt

# 3. Настроить private.txt
# (добавить свои ключи)

# 4. Запустить
python main.py

# 5. Использовать в Telegram
# Отправить: /analyze
```

### На Ubuntu сервере

```bash
# 1. Клонировать
git clone <url> && cd chat-filter

# 2. Настроить
pip3 install -r requirements.txt
nano private.txt

# 3. Запустить в фоне
chmod +x *.sh
./start_bot_background.sh

# 4. Отключиться от SSH
# Бот продолжит работать!
```

---

## 🔧 Решение проблем (быстро)

| Проблема | Решение |
|----------|---------|
| `ModuleNotFoundError` | `pip install -r requirements.txt` |
| Бот не реагирует | Проверить, что команда отправлена от вашего имени |
| Неправильный API_ID | Убрать кавычки в `private.txt` |
| Запрос кода 2FA | Добавить `password` в `telegram_client.start()` |
| Нет прав на запись | `chmod +w .` |
| Screen не найден | `sudo apt-get install screen` |
| Длинный результат | Автоматически разбивается на части |
| Закончились кредиты | Пополнить баланс Perplexity |

---

## 📊 Примеры использования

### Рабочий чат
```
/analyze 1d    # Что было вчера
/analyze 8h    # За рабочий день
/analyze 5d    # За рабочую неделю
```

### После отпуска
```
/analyze 14d   # 2 недели
```

### Перед встречей
```
/analyze 6h    # Последние обсуждения
```

### Еженедельный отчет
```
/analyze 7d    # Неделя
```

---

## 🔗 Полезные ссылки

| Ресурс | URL |
|--------|-----|
| Telegram API | https://my.telegram.org/auth |
| Perplexity API | https://www.perplexity.ai/settings/api |
| Telethon Docs | https://docs.telethon.dev/ |
| Perplexity Docs | https://docs.perplexity.ai/ |

---

## 💡 Полезные команды Git

```bash
# Обновить код на сервере
git pull

# Проверить статус
git status

# Откатить изменения
git checkout main.py

# Посмотреть изменения
git diff

# Создать ветку для экспериментов
git checkout -b experimental
```

---

## 🎯 Горячие клавиши Screen

| Комбинация | Действие |
|------------|----------|
| `Ctrl+A` → `D` | Отключиться от сессии |
| `Ctrl+A` → `K` | Убить текущее окно |
| `Ctrl+A` → `?` | Показать помощь |
| `Ctrl+A` → `[` | Режим прокрутки (Esc для выхода) |

---

## 📋 Чеклист перед развертыванием

- [ ] Клонирован репозиторий
- [ ] Установлены зависимости
- [ ] Создан `private.txt` с ключами
- [ ] API_ID без кавычек
- [ ] Телефон в формате `+79001234567`
- [ ] Протестирован запуск `python main.py`
- [ ] Получен код от Telegram
- [ ] Команда `/analyze` работает
- [ ] Настроен автозапуск (опционально)
- [ ] `private.txt` в `.gitignore`

---

## 🆘 Быстрая помощь

```bash
# Посмотреть логи
tail -f bot.log

# Проверить процесс
ps aux | grep python

# Убить процесс
pkill -f main.py

# Проверить порты
netstat -tulpn | grep python

# Освободить место
rm analysis_*.json
```

---

## 📱 Режимы работы

| Режим | Команда | Когда использовать |
|-------|---------|-------------------|
| Интерактивный | `python main.py` | Разработка, отладка |
| Screen | `./start_bot_background.sh` | Простое развертывание |
| Systemd | `systemctl start telegram-bot` | Production, автозапуск |
| Docker | `docker-compose up -d` | Изоляция, масштабирование |

---

## ⚡ Производительность

### Примерное время анализа

| Сообщений | Время |
|-----------|-------|
| 10-50 | 5-10 сек |
| 50-200 | 10-30 сек |
| 200-500 | 30-60 сек |
| 500+ | 1-3 мин |

### Примерная стоимость (Perplexity)

| Анализ | ~Стоимость |
|--------|------------|
| 1 чат (100 сообщений) | $0.01-0.03 |
| 10 чатов | $0.10-0.30 |
| 100 чатов | $1-3 |

*Цены примерные, зависят от модели и длины сообщений*

---

## 🎨 Кастомизация

### Изменить промт
Файл: `main.py` → переменная `ANALYSIS_PROMPT`

### Изменить период по умолчанию
Файл: `main.py` → функция `handle_analyze_command` → `hours = 24`

### Изменить модель AI
Файл: `main.py` → функция `create_summary` → `model='sonar'`

### Изменить лимиты
```python
max_chars = 15000      # Максимум символов для анализа
max_tokens = 2000      # Максимум токенов в ответе
max_length = 4096      # Максимум символов в сообщении Telegram
```

---

Для подробной информации смотрите:
- 📖 [README.md](README.md) - Полная документация
- 📝 [USAGE_EXAMPLES.md](USAGE_EXAMPLES.md) - Примеры использования
- 🔧 [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем

